# Demo terraform project that uses LocalStack (AWS)

Download Terraform binary and place it in project root

### Commands to execute:
- `./terraform init`
- `./terraform plan`
- `./terraform apply -auto-approve`
- `./terraform destroy`